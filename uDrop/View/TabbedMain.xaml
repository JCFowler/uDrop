<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    x:Class="uDrop.View.TabbedMain"
    Title="Main" Icon="ic_home.png">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ContentView Grid.Column="0" Grid.Row="0" BackgroundColor="Aqua"  >
            <Button x:Name="dropBtn" Text="Drop" Clicked="Drop_Clicked" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center"/>
        </ContentView>

        <ContentView Grid.Column="0" Grid.Row="1" BackgroundColor="Red" >
            <Button Text="Pick Up" FontSize="Large" HorizontalOptions="Center" VerticalOptions="Center" Clicked="PickedUp_Clicked"/>
        </ContentView>
        
    </Grid>
</ContentPage>
